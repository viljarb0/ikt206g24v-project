# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Allow any pod in the cluster to connect with no password (INSECURE — use only in staging)
host    all             all             0.0.0.0/0               trust
